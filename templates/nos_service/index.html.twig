{% extends 'base.html.twig' %}

{% block nav %}
    {% for categorie in categories %}
        <li id="nav-item" class="nav-item">
            <a class="nav-link active" href="{{ path('second_page', {'id': categorie.id} ) }}"
            >{{ categorie.nom}} <span class="sr-only">(current)</span></a
            >
        </li>
    {% endfor %}
{% endblock %}
{% block body %}
<main>
    <div class="container">
        <div class="row">
            <div class="box-c1">
            <section class="services">
        Les Services de Zitouna concept

        <div class="row">Tapissage Chaise</div>

        <div class="row">Réparation Chaise</div>

        <div class="row">service de déménagement</div>

        <div class="row">Plan D'installation 2D/3D</div>
    </section>
            </div>
        </div>
    </div>
</main>
{% endblock %}
